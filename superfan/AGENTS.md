# Agents

## After Every Code Change

You MUST run all three checks before considering any task complete:

```bash
npm run lint
npm run build
npm run test
```

All three must pass. Fix any failures before finishing.
